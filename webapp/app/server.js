window.NC = {};
window.NC.Progress = new Progress();

window.NC.REST = {
	propertyLet: server + '/property/' + workspace + '/',
	nodeLet: server + '/node/' + workspace,
	valueLet: server + '/value/' + workspace

};
